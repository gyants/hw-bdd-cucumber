<h2>Search Results for '<%= @query %>'</h2>

<style>
  .movie-card {
    background-color: #f5f5f5;
    border: 1px solid #ddd;
    border-radius: 5px;
    margin-bottom: 20px;
    padding: 20px;
    cursor: pointer;
  }

  .movie-card:hover {
    background-color: #e0e0e0;
  }

  .movie-title {
    font-size: 1.5rem;
    margin-bottom: 10px;
  }

  .release-date {
    font-size: 0.9rem; /* Smaller font size */
    color: #888; /* Lighter text color */
    margin-bottom: 10px;
  }
</style>

<% if @movie_results.any? %>
  <div class="movie-cards">
    <% @movie_results.each do |movie| %>
      <div class="movie-card" onclick="window.location.href='<%= select_movie_path(title: movie["title"], release_date: movie["release_date"], overview: movie["overview"], rating: movie["vote_average"].to_s) %>'">
        
        <div class="movie-title">
          <strong><%= movie["title"] %></strong>
        </div>

        <div class="release-date">
          <% if movie["release_date"].present? %>
            <%= Time.parse(movie["release_date"]).strftime('%d %B %Y') %>
          <% else %>
            Release date not available
          <% end %>
        </div>

        <div class="movie-info">
          <p><%= movie["overview"] %></p>
        </div>

      </div>
    <% end %>
  </div>
<% else %>
  No results found.
<% end %>
